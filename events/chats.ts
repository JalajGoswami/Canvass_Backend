import { WS_Server, WS_Socket } from '../types/WebSocketTypes'

export default function registerHandlers(ws: WS_Server, socket: WS_Socket) {
    // activeUsers: Record<string, number> = {}

    // socket.on('new_user', id => {
    //     activeUsers[socket.id] = id
    // })
}